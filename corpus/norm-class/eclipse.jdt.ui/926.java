i some if isomeif i some if isomeif f some if fsomeif i some if isomeif f some if fisomeif returns i some if isomeif i some if isomeif get some if getisomeif f some if fisomeif param set some if setisomeif i some if isomeif f some if fisomeif returns some if someif i some if isomeif get some if getsomeif f some if fsomeif param set some if setsomeif i some if isomeif f some if fsomeif